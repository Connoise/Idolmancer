import React from 'react';
import ReactDOM from 'react-dom/client';
import MusicTheoryApp from './MusicTheoryApp';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <MusicTheoryApp />
  </React.StrictMode>,
);
