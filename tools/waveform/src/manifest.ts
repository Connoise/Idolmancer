import type { ToolManifest } from '@idolmancer/data-model';

export const manifest: ToolManifest = {
  id: 'waveform',
  name: 'Waveform',
  category: 'analysis',
  version: '0.1.0',
  icon: 'AudioWaveform',
};
